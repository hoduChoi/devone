- 로또.

1. 1부터 45까지 숫자 중 랜덤으로 6개 생성되어야 한다.
2. 중복되는 수는 없어야 한다.
3. 보너스 숫자도 추첨되게 해야한다.


1차.
간단한 자동 로또 구현.
돈 입력 후 개수에 맞게 추출 
45개 셔플 후 6개 추출해줌.

2차.
Math.random 사용.

3차. 
Set으로 중복제거

4차.
클래스.

5차.
랜덤 확률로 6개의 문자가 찍히도록 하였고, 각 문자가 찍힌 후에 쓰레드 이용하여 약 1초간 정지 후 
다시 프로그램 작동 마지막에는 배열 출력 후 프로그램 반복되도록.
금주의 로또 발표자, 로또 볼, 뽑힌 번호가 담긴배열.

6차.
