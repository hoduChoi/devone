#영웅
1. 영웅은 다음과 같은 속성을 갖는다.
-이름, 체력
-영웅은 펀치를 통해 피해 입을 수 있다.

#연봉
1.개발자의 연봉 구하는 프로그램

- 개발자는 이름과 경력 정보를 갖는다.
- 경력에 따라 초금/중급/고급 단계로 나뉜다.
- 연봉은 공식이 주어짐.
연봉 = 단계별 기준연봉 + (100 * 경력) 
단계별 기준연봉은 초급(2800) / 중급(3500) / 고급(4500)으로 함.
developer - name, carrer / level(), salary()

#Circle
CircleTest 클래스: 원 객체, circle 반지름 2로 수정, 1~4범위 내 정수 
				circle1출력, circle2출력, circle1의 면적을 출력 circle2의 면적 출
Circle클래스 필드: 반지름, 매개변수가 없는 디폴트 생성자(반지름 1로 초기화), 
				반지름을 매개변수로 하는 생성자.(입력값으로 반지름 초기화)
				반지름 게터 세터 반지름 반환, 수정
				원 면적 계산하는 메소드 면적 = 반지름 * 반지름 * Math.PI
				toString 메소드 : 반지름 정보 문자열로.
				
#Meeting
User 클래스 
회원 객체 생성 (0.0 ~ 5.0)


#한 주간 일한 시간.
