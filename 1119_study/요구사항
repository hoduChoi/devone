학사관리 프로젝트
학생 정보 관리와 교사 정보 관리에 대한 프로그램

학생은 학번, 이름, 성별 등을 확인할 수 있다.

학생 정보
- 학번, 이름, 성별
교사 정보
- 교사번호, 이름, 성별, 담당과목 

#상속
:클래스 간의 멤버들이 같은 것을 재사용함.
학생: 학번, 성별, 이름, 전화번호, 프린트
교사: 교번, 성별, 이름, 전화번호, 급여, 프린트 

공통: 성별, 이름, 전화번호, 프린트

공통 멤버를 갖는 클래스를 별도로 작성. 
기존 클래스에는 공통 멤버 추출 -> 공통 클래스에는 기존 클래스가 상속. 
** 클래스의 단일 상속만 제공.
상속의 장점/단점: 외부 클래스의 멤버 재사용 가능, 강한 결합도 

공통 클래스(person) - 학생 클래스	
					교사 클래스   
					
					
**메모리 구조 그려봄. 
*new를 하면 무조건 메로리가 잡히고 연결고리가 끊어지면 사라질 대상이 된다.
메모리의 연결고리가 없다는 것은 쓰레기 데이터가 쌓였다는 건데 일을 하는 것이 먼저이기 때문이다.
GCT(Garbege Collector Thread)

*Object: 모든 클래스의 최상위 클래스 
toString() 재정의: 자원 문자열로 관리. 단 중요한 것은 노출 안함., 
equals(), hashCode() 재정의: 세트로 움직이는 애들.